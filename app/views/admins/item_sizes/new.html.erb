<div class="container mt-3">
	<div class="row">
		<div class="colcol-6 mx-auto">

			<!-- Zone du formulaire -->
			<%= form_tag url_for(action: 'create'), method: 'post' do %>
			
			<h4><%=@item.name%></h4>

			
			<%= hidden_field_tag "item_id", @item.id %>
		
				<%= render 'item_sizes_form' %>     

			<%= submit_tag "Enregistrer", class: "btn btn-dark" %>
			
			<%end%>
			
		</div>
	</div>
	
</div>


